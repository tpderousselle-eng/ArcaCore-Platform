# Additional runtime dependency for generated Pydantic 2 EmailStr fields.
# Install in the existing project virtual environment and any generated app
# environment that uses format=email.
email-validator>=2.3,<3
